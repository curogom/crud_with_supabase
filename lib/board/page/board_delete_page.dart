import 'package:flutter/material.dart';

class BoardDeletePage extends StatelessWidget {
  final String? id;

  const BoardDeletePage({super.key, this.id});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
